# mtgpu out-of-tree linux driver

## Unmaintained

Due to the new loginwall it applies, I won't keep maintaining this. Maybe mainline kernel user donot need a Moore Threads GPU.

## 不再维护

因为新加了登录下载，我不会继续维护这个项目，可能主线用户并不需要MTT的显卡
